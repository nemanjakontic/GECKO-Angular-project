import { ChampionPipePipe } from './champion-pipe.pipe';

describe('ChampionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
